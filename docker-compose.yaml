include:
  - ./docker-compose-napcat.yaml
services:
  agibot:
    image: localhost/agibot:0.1.0
    env_file:
      - .env
    ports:
      - 8080:8080
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
